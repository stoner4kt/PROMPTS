# You are analyzing a web application repository for a mobile PWA

You are analyzing a web application repository for a mobile PWA. The goal is to fix a bug where vehicle inspection signatures captured on the frontend are not appearing on the final PDF documents generated by the system.

Context:

  - Signatures are captured using SignaturePad.
  - This data is converted into Base64 image strings.
  - pdf-lib is used to compile/download, but signatures are missing.

Examine repository files to:

1.  Traces the signature data flow.
2.  Identify where pdf-lib compiles pages and signature coordinates.
3.  Find why Base64 string is not embedded correctly.
4.  Provide the code fix to render signatures on the PDF.

---

Generate me a prompt I can feed into codex/replit to make these necessary changes to my system without changing anything else or breaking the functionality of the current system, so that the system is production ready
