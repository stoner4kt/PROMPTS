# This is my system I built https://github

This is my system I built https://github.com/stoner4kt/Fleet-system.git I want to integrate Sage the invoicing system so that we a admin creates a invoice, they don't have to hop between systems to view or use the invoice a so that the invoice number generated by the system matches the number that get from the sage system

---

So it's a fleet management system with bookings and inspections, analyze the system , my schema is at https://github.com/stoner4kt/SCHEMAS-AND-SCRIPTS/tree/main/Inyathi/supabase_all.sql
The client currently uses sage to generate invoice for bookings they create in the system so it's like 2 separate documents that they have to create in 2 separate places and the bookings get a invoice number generated by the system and safe generates thier own invoice number. What would be the best way to handle these

---

Okay so i want the invoice to be created along with the booking so that they have the same invoice number so that they are easily searchable and So that bookings don't lose their respective invoices
