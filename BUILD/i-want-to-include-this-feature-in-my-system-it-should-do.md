# I want to include this feature in my system, it should do

I want to include this feature in my system, it should do the same as the current system and write to notion after writing to supabase, and I should have the option of switching the agent on and off, have a dashboard to view/remove leads, This is my system https://github.com/stoner4kt/Conextsol-Agencyv2.git and this is the feature I want added : Act as a Senior Staff Engineer specializing in serverless architectures and web automation. I need you to write a complete, production-ready Cloudflare Worker script that acts as an automated morning lead generation agent for my web development agency. 

Crucially, the script must handle BOTH the daily scraping routine AND an automated initialization routine that creates the required Notion database schema programmatically so I do not have to set up columns manually and sql schema I need to run

Please provide the complete project file structure, including the `wrangler.toml` file and the `src/index.js` file with all the code written out fully. Do not use placeholders or comments like "// implement later".

### 1. System Architecture & Routing Requirements

- Environment: Cloudflare Workers (Free Tier optimized).
- Runtime: Vercel/Deno edge-compatible JavaScript (ES Modules).
- Trigger 1: Scheduled Cron event (set for every morning at 07:00 UTC) to run the lead generation scraper automatically.
- Trigger 2: HTTP Fetch event that handles routing based on the URL path:
  * GET `/init-database`: Executes the automated Notion database creation script.
  * GET `/run`: Manually forces a lead scraping run for testing and local development.

### 2. Automated Notion Database Creation Script (`/init-database`)
When the `/init-database` endpoint is hit, the worker must send a POST request to `https://notion.com` to programmatically create a database under a parent page.
- Target Parent: `env.NOTION_PARENT_PAGE_ID`
- Database Title: "Automated Agency Leads"
- Properties (Columns) Schema to build programmatically:
  * "Lead Name": Define as a `title` object type.
  * "Source Platform": Define as a `select` object type (with predefined options: "Reddit", "Upwork Feed", "Niche Directory Site").
  * "Origin URL": Define as a `url` object type.
  * "Details Summary": Define as a `rich_text` object type.
  * "Status": Define as a `status` object type (defaulting to "Not Started" / "Inbox").
- The response will return a new `id` (the Database ID). Have the worker return this string in the HTTP response body so I can copy it.

### 3. Scraping Targets & Specifications
When the morning cron or `/run` path executes, the agent must run three distinct scraping jobs simultaneously using Promise.allSettled:

A. Target 1: Reddit API (Active Intent)
- Fetch recent 25 posts from: https://reddit.com
- Include a custom HTTP User-Agent header to prevent API blocking.
- Match posts against lowercase keywords: ["need a website", "web developer", "redesign", "wordpress help", "hire a developer"].
- If a match is found, format it into the unified schema.

B. Target 2: Upwork Feed (Active Intent)
- Fetch the public Recency Atom/RSS feed: https://upwork.com
- Implement a robust Regular Expression (RegEx) parser suitable for serverless worker environments to loop through individual `<entry>` tags.
- Extract the raw string values inside `<title>`, `<link href="...">`, and `<content>`. Clean and strip out raw HTML formatting from the text.

C. Target 3: Static Niche Directory (Passive Scraping)
- Launch a headless browser instance using Cloudflare Browser Rendering via `puppeteer.launch(env.MY_BROWSER)`.
- Navigate to 'https://example-niche-directory.com' with a strict 'domcontentloaded' wait threshold.
- Evaluate the DOM to extract data from a structured wrapper class called `.business-card-selector`. Capture child nodes `.company-name-selector` (Title), `.website-link-selector` (URL), and `.contact-info-selector` (Description).
- Include an imperative `finally {}` block to guarantee `browser.close()` runs to prevent dangling headless instances.

### 4. Data Sync Pipeline
Iterate through all gathered leads and push them into the programmatically created Notion Database via `https://notion.com`. 
- Map properties to match the exact database schema names created in Step 2.
- Enforce string boundaries: Max 100 characters for Title, Max 2000 characters for Details Summary.

### 5. Configuration Security
Do not hardcode sensitive operational credentials. Read them entirely through the Worker global environment object (`env`):
- `env.NOTION_PARENT_PAGE_ID` (The ID of your workspace page where the database will live)
- `env.NOTION_DATABASE_ID` (The ID generated by the initialization path, used by the daily scraper script)
- `env.NOTION_INTEGRATION_TOKEN`
- Ensure all Notion API calls use the header `Notion-Version: 2022-06-28`.

### 6. Final Output
Provide the implementation across two files:
1. `wrangler.toml`: Set up the project configuration, compatibility date, cron block, browser bindings, and environment variables slots.
2. `src/index.js`: The comprehensive production-ready codebase featuring routing switches, robust try/catch logic, parsing logic, and secure error boundaries.

---

Generate me a prompt i can feed into cluade/codex to analyze my repo and make all these changes to my system:
The prompt should instruct cluade to create the notion databases and provide me with the id's
The sql schema I need to run in supabase
Tell exactly what secrets to set where 
Then cluade should generate a separate prompt i can feed into codex/Google Ai studio to make the UI changes and the dashboard , add the github workflow and workers so they deploy automatically
