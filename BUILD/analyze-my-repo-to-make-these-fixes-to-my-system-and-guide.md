# Analyze my repo to make these fixes to my system, and guide

Analyze my repo to make these fixes to my system, and guide me what to do https://github.com/stoner4kt/ToursystemV2refactored.git  :  (When i try to request a edit on the Transfer Recon sheet on the drivers side it should send a edit but the function fails, (it logs the request but doesn't notify the admin) I am using the the send-otp-mail function but I switch it to (drivers request edits admin is notified and approve or rejecting in dashboard and the driver get to edit that sheet it currently does that but still sends the otp but I want it to send a alert rather with that drivers information like I currently do in the systems for fault-alerts on the inspection sheet ) And on the admins side when I approve a Admin does a director sign off it shows : (Authorize action : transfer_approval?) And    when admin approve or rejecting a edit is says Authorize action : review_edit_request

My supabase schema is at supabase/schema/schema-latest.sql
And my functions are at supabase/functions/** )
