# This is my sql schema I want to be able to send

This is my sql schema I want to be able to send it to my friend and they should be able to run it without getting errors, it currently has a lot of errors, fix that especially the declaring of public as it's default in supabase

---

Will this recreate the database exactly the same as the one I pulled the dump from because the handle user trigger is directed at the profiles table in my database triggers

---

Okay so I can run this schema in a new project an is that the only thing I have to doing

---

Okay so I don't use storage buckets as I use cloudinary with edge functions, the fields stores the URLs

---

Okay so the clean _expired_otps should have a crown job that cleans the otp_verifications table every 30 days

---

Can you insert this into the schema so I can run the schema in a fresh repo and only run the handle_new_user outside of the schema

---

So I can now run this schema annit will recreate the exact database from the dump without data in it

---

I'm getting this error when I run the script in my supabase SQL editor: Error failed to run SQL query error 3f000 no schema has been created to select it

---

It's still giving that error
