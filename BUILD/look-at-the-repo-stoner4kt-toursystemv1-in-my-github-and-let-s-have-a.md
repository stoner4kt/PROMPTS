# Look at the repo stoner4kt/ToursystemV1 in my github and let's have a

Look at the repo stoner4kt/ToursystemV1 in my github and let's have a discussion

---

Is there a open source repo that is already built like this system

---

Would it say this if I'm trying to delete a vehicle that is tied to a active booking

---

So if i run this my system will still work, the tying of vehicles to inspection sheet ( ALTER TABLE public.inspections
  DROP CONSTRAINT IF EXISTS fk_inspections_vehicles;

ALTER TABLE public.inspections
  DROP CONSTRAINT IF EXISTS inspections_vehicle_reg_fkey;)
