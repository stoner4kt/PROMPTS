# You are an expert frontend developer and technical analyst

You are an expert frontend developer and technical analyst.

The website is a **pure vanilla HTML, CSS, and JavaScript** project (no React, no frameworks) located at:  
https://github.com/stoner4kt/Appliance-911-LIVE/tree/main 

**Your job has two parts:**

### Part 1: Analysis
- Explore the full repository structure.
- Focus especially on the **assets** folder and list every image file inside it with exact filenames.
- Identify all HTML pages in the project (homepage, service pages like fridge, washer, dryer, etc.).
- Note the current structure of the pages (especially hero sections, headers, background usage, and any existing `<img>` tags).

### Part 2: Generate a Precise Action Prompt
After your analysis, output **one single, highly detailed prompt** that I can copy-paste into Replit Agent, Claude Code, Cursor, or any other code-editing agent.

That generated prompt must:
- Tell the agent to clone or work on the repo `stoner40/appliance-911-live`
- Instruct it to map every image from the assets folder to its correct page (fridge images → fridge service page, homepage images → index.html, etc.)
- Give clear, file-by-file instructions for adding the images as hero banners, backgrounds, content images, etc.
- Include exact code snippets for HTML and CSS changes (since it's vanilla HTML/CSS/JS).
- Specify responsive design, proper `alt` text, correct relative paths, and best practices.
- End with steps to test the changes locally.

Be extremely thorough and specific in the prompt you generate. Base it directly on what you discover in the repo.

Start your response with the analysis summary, then clearly separate it with:

---

**FINAL PROMPT TO COPY:**

[Insert the full ready-to-use prompt here]

---

---

Generate me the prompt in a downloadable MD file
