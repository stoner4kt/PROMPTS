# Also when a admin enters a tour reference & invoice number the

Also when a admin enters a tour reference & invoice number the system shouldn't allow them to create a booking if those exist already in the system.
On the booking form. Analyze the repo and Generate me a prompt I can feed into codex/replit to make these changes to the system
